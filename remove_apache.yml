---

- hosts: ubuntus
  become: true
  tasks:

  - name: update repository index
    apt:
      update_cache: yes

  - name: uninstall apache2 package and the rest
    apt:
      name: 
        - apache2
        - libapache2-mod-php
      state: absent
